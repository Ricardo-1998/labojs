function usuario(nombre, apellido, fecha, tel, email) {
    
    var user ={
        nombre: nombre,
        apellido: apellido,
        fecha: fecha,
        tel: tel,
        email:email
    };

    console.log(user);
    
  
}